

---

## 🖥️ 系统管理命令

- **ip a** → 查看网络接口和 IP 地址  
    示例：`ip a`
    
- **ifup / ifdown** → 启用或禁用网络接口  
    示例：`ifdown eth0 或 ifup eth0`
    
- **nmcli** → 管理网络连接  
    示例：`nmcli device status`
    
- **reboot** → 重启系统  
    示例：`reboot`
    
- **init** → 改变运行级别  
    示例：`init 0` (关机)
    
- **systemctl** → 管理服务和系统状态  
    示例：`systemctl status httpd`
    
- **shutdown** → 关机或重启  
    示例：`shutdown -h now`
    
- **poweroff** → 立即关机  
    示例：`poweroff`
    
- **halt** → 停止所有进程并关机  
    示例：`halt`
    

---

## 👤 用户与权限管理

- **passwd** → 修改用户密码  
    示例：`passwd`
    
- **useradd / userdel** → 添加/删除用户  
    示例：`useradd user01`
    
- **usermod** → 修改用户属性  
    示例：`usermod -aG wheel user01`
    
- **id** → 查看用户 UID、GID  
    示例：`id user01`
    
- **groups** → 查看用户组  
    示例：`groups user01`
    
- **groupadd / groupdel** → 添加/删除用户组  
    示例：`groupadd devteam`
    
- **chmod** → 修改权限  
    示例：`chmod 755 file.txt`
    
- **chown** → 修改文件属主  
    示例：`chown user:group file.txt`
    
- **su** → 切换用户  
    示例：`su - root`  ## - 或 -l 为完整登录
    
- **sudo** → 以管理员执行命令  
    示例：`sudo ls /root`
    

---

## 📂 文件与目录管理

- **pwd** → 显示当前路径  
    示例：`pwd`
    
- **ls** → 列出文件  
    示例：`ls -l`
    
- **cd** → 切换目录  
    示例：`cd /etc`
    
- **mkdir** → 创建目录  
    示例：`mkdir dir01`
    
- **rmdir** → 删除空目录  
    示例：`rmdir dir01
    
- **touch** → 创建空文件  
    示例：`touch file.txt`
    
- **cp** → 复制文件  
    示例：`cp file1.txt file2.txt`
    
- **mv** → 移动/重命名文件  
    示例：`mv file.txt /tmp/`
    
- **rm** → 删除文件  
    示例：`rm file.txt`
    
- **cat** → 查看文件内容  
    示例：`cat file.txt`
    
- **less** → 分页查看文件  
    示例：`less /etc/passwd`
    
- **head** → 查看前几行  
    示例：`head -n 5 file.txt`
    
- **tail** → 查看末尾内容  
    示例：`tail -f /var/log/messages`
    
- **stat** → 查看文件详细信息  
    示例：`stat file.txt`
    
- **tree** → 树状显示目录结构(需安装tree)  
    示例：`tree /etc`
    

---

## 📦 压缩与归档

- **tar** → 归档/解归档  
    示例：`tar -czf archive.tar.gz dir/`
    
- **gzip / gunzip** → 压缩/解压缩  
    示例：`gzip file.txt`
    
- **zip / unzip** → 压缩/解压缩 zip 文件  
    示例：`zip archive.zip file1 file2`
    

---

## 🔎 查找与定位

- **which** → 查找可执行文件路径  
    示例：`which ls`
    
- **whereis** → 查找命令相关文件  
    示例：`whereis bash`
    
- **locate** → 按数据库查找文件  
    示例：`locate passwd`
    
- **find** → 文件搜索  
    示例：`find /etc -name "hosts"`
    

---

## 💾 磁盘与存储

- **lsblk** → 列出块设备  
    示例：`lsblk`
    
- **fdisk / gdisk** → 分区工具  
    示例：`fdisk /dev/sda`
    
- **parted** → 磁盘分区管理  
    示例：`parted /dev/sda`
    
- **mkfs.ext4** → 格式化分区为 ext4  
    示例：`mkfs.ext4 /dev/sda1`
    
- **mkfs.xfs** → 格式化分区为 xfs  
    示例：`mkfs.xfs /dev/sda1`
    
- **mount / umount** → 挂载/卸载文件系统  
    示例：`mount /dev/sda1 /mnt`
    
- **df** → 显示磁盘使用情况  
    示例：`df -h`
    
- **du** → 查看目录大小  
    示例：`du -sh /var/log`
    

---

## 🌐 网络管理

- **ifconfig** → 查看配置网络接口 (旧命令)  
    示例：`ifconfig`
    
- **ping** → 测试网络连通性  
    示例：`ping -c 4 8.8.8.8`
    
- **tracepath** → 路由跟踪  
    示例：`tracepath www.google.com`
    
- **iptables** → 配置防火墙规则  
    示例：`iptables -L`
    
- **firewall-cmd** → 管理 firewalld  
    示例：`firewall-cmd --list-all`
    

---

## 📊 进程与性能

- **ps** → 查看进程  
    示例：`ps aux`
    
- **top** → 动态查看进程  
    示例：`top`
    
- **kill** → 终止进程  
    示例：`kill -9 1234`
    
- **pkill** → 按进程名杀掉进程  
    示例：`pkill httpd`
    
- **uptime** → 查看开机时长  
    示例：`uptime`
    
- **free** → 查看内存使用  
    示例：`free -h`
    
- **lscpu** → 查看 CPU 信息  
    示例：`lscpu`
    

---

## 📅 计划任务与日志

- **crontab** → 定时任务  
    示例：`crontab -e`
    
- **at** → 定时执行一次任务  
    示例：`echo "ls /" | at now + 1 min`
    
- **last** → 最近登录记录  
    示例：`last`
    
- **lastlog** → 查看所有用户的最后登录  
    示例：`lastlog`
    
- **logrotate** → 管理日志轮换  
    示例：`logrotate -d /etc/logrotate.conf`
    

---
