基础命令
cd     cd /tmp  cd tmp
ls     ls /tmp    -a -d -l -h
pwd
hostname      hostname node1
cat   tac    head  tail  more less   # 查看文件内容
ip a
ping
yum/dnf    yum -y install vim         /  yum -y remove vim
tar       tar -axf hadoop.tar.gz -C /usr/local                      -acf（打包）
mkdir    mkdir  -p aaa/bbb/ccc 
touch   touch /tmp/1.txt
cp  -r  -a
mv   (原地移动---更名)
rm      rm -rf  /aaa /tmp/1.txt
useradd    useradd hadoop
systemctl      systemctl  start|stop|restart|reload|enable|disable|status    network
vi vim
 ：（冒号）wq（保存退出）<-------一进去就是命令模式（默认）---->i或a或o 进入插入模式（写东西）
                   ---------------> esc                                                                         esc <------ 
netstat -tunlp   #查看当前服务器监听端口
date   # 查看日期时间
chmod #修改权限
chown  #改文件属主属组
rpm  #rpm包管理工具
rpm -ivh 软件包名称   #安装软件包   （不能解决依赖关系）
主要用来查询
rpm -qa # 查看安装的所有软件包
rpm -qa | grep vim   #配合grep使用，查询指定包是否安装
wget  #下载工具   wget  下载路径
例：wget https://dev.mysql.com/get/mysql84-community-release-el7-2.noarch.rpm
grep #过滤关键字
scp  #跨主机复制
例：scp /etc/yum.repos.d/CentOS-Base.repo 192.168.100.20:/etc/yum.repos.d/
make
make install
wc
sort
uniq
cut
read
sleep
clear
grep
sed
awk

free -h #看内存
df -h  #查看磁盘使用情况
lscpu  #查看cpu的信息
lsblk  #看块设备
top #动态查看 cpu负载、进程信息、cpu、内存使用情况
ps  #看进程信息
vmstat # 主要看磁盘io










[[kvm虚拟平台]]






















