环境：
server1   192.168.100.10   服务端
server2   192.168.100.20   客户端
1、按规划设置好主机名和静态ip地址
2、配置服务端（该服务默认已经安装）
修改配置文件：/etc/chrony.conf
........allow开头这一行，在下面新增一行
allow  all
............

3、重启服务
systemctl restart chronyd
netstat -tunlp   #  查看端口，新增端口123

4、客户端配置
修改配置文件  /etc/chrony.conf
删除4行：
server 0.centos.pool.ntp.org iburst
server 1.centos.pool.ntp.org iburst
server 2.centos.pool.ntp.org iburst
server 3.centos.pool.ntp.org iburst

换成：
server 192.168.100.10 iburst   #换成自己服务端地址

5、测试效果
date  #查看时间
date -s "2000-1-1"  #修改客户端时间
systemctl restart chronyd  #客户端重启服务去同步时间
date  #观察时间是否同步成功





[[DNS服务配置]]